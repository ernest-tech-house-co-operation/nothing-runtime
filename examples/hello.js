// examples/hello.js
// Run with: nth hello.js  (or: nth hel   via prefix-match)
console.log("Hello from nth — JavaScriptCore embed.");
console.log("1 + 2 =", 1 + 2);

const arr = [1, 2, 3, 4, 5];
const sum = arr.reduce((a, b) => a + b, 0);
console.log("sum =", sum);
